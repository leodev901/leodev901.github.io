import PublicScrapClient from "@/app/components/studio/PublicScrapClient";

export const metadata = {
    title: "Scrap | Leo - Senior Full-Stack & AI Engineer",
};

export default function Scrap() {
    return <PublicScrapClient />;
}
